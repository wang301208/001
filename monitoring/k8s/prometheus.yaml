apiVersion: v1
kind: ConfigMap
metadata:
  name: assistant-prometheus-config
  labels:
    app: assistant-prometheus
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
    rule_files:
      - /etc/prometheus/rules/assistant-alerts.yml
    scrape_configs:
      - job_name: assistant-gateway
        metrics_path: /metrics
        static_configs:
          - targets:
              - assistant:3000
      - job_name: assistant-health
        metrics_path: /healthz
        static_configs:
          - targets:
              - assistant:3000
  assistant-alerts.yml: |
    groups:
      - name: assistant.rules
        rules:
          - alert: AssistantGatewayDown
            expr: up{job="assistant-gateway"} == 0
            for: 2m
            labels:
              severity: critical
            annotations:
              summary: Assistant gateway is down
              description: Prometheus cannot scrape the assistant gateway for more than two minutes.
          - alert: AssistantHealthProbeDown
            expr: up{job="assistant-health"} == 0
            for: 2m
            labels:
              severity: warning
            annotations:
              summary: Assistant health endpoint is unavailable
              description: The /healthz endpoint is not responding through the service endpoint.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assistant-prometheus
  labels:
    app: assistant-prometheus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: assistant-prometheus
  template:
    metadata:
      labels:
        app: assistant-prometheus
    spec:
      automountServiceAccountToken: false
      containers:
        - name: prometheus
          image: prom/prometheus:v2.55.1
          args:
            - --config.file=/etc/prometheus/prometheus.yml
            - --storage.tsdb.path=/prometheus
            - --web.enable-lifecycle
          ports:
            - name: http
              containerPort: 9090
          readinessProbe:
            httpGet:
              path: /-/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus/prometheus.yml
              subPath: prometheus.yml
            - name: config
              mountPath: /etc/prometheus/rules/assistant-alerts.yml
              subPath: assistant-alerts.yml
            - name: data
              mountPath: /prometheus
      volumes:
        - name: config
          configMap:
            name: assistant-prometheus-config
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: assistant-prometheus
  labels:
    app: assistant-prometheus
spec:
  type: ClusterIP
  selector:
    app: assistant-prometheus
  ports:
    - name: http
      port: 9090
      targetPort: http
