apiVersion: v1
kind: ConfigMap
metadata:
  name: zhushou-grafana-dashboards
  labels:
    app: zhushou-grafana
data:
  zhushou-overview.json: |
    {
      "editable": true,
      "panels": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": {
            "defaults": {
              "color": { "mode": "thresholds" },
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "red", "value": null },
                  { "color": "green", "value": 1 }
                ]
              }
            },
            "overrides": []
          },
          "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
          "id": 1,
          "options": {
            "colorMode": "value",
            "graphMode": "area",
            "justifyMode": "auto",
            "orientation": "auto",
            "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
            "textMode": "auto"
          },
          "targets": [
            { "expr": "up{job=\"zhushou-gateway\"}", "legendFormat": "gateway", "refId": "A" }
          ],
          "title": "Gateway Availability",
          "type": "stat"
        }
      ],
      "refresh": "30s",
      "schemaVersion": 39,
      "tags": ["zhushou", "operations"],
      "time": { "from": "now-6h", "to": "now" },
      "title": "Zhushou Overview",
      "uid": "zhushou-overview",
      "version": 1
    }
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zhushou-grafana-provisioning
  labels:
    app: zhushou-grafana
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        uid: prometheus
        access: proxy
        url: http://zhushou-prometheus:9090
        isDefault: true
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: zhushou
        orgId: 1
        folder: Zhushou
        type: file
        disableDeletion: false
        allowUiUpdates: true
        options:
          path: /var/lib/grafana/dashboards
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zhushou-grafana
  labels:
    app: zhushou-grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zhushou-grafana
  template:
    metadata:
      labels:
        app: zhushou-grafana
    spec:
      automountServiceAccountToken: false
      containers:
        - name: grafana
          image: grafana/grafana:11.3.1
          env:
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zhushou-secrets
                  key: GRAFANA_ADMIN_PASSWORD
                  optional: true
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          volumeMounts:
            - name: dashboards
              mountPath: /var/lib/grafana/dashboards
            - name: provisioning
              mountPath: /etc/grafana/provisioning/datasources/datasources.yaml
              subPath: datasources.yaml
            - name: provisioning
              mountPath: /etc/grafana/provisioning/dashboards/dashboards.yaml
              subPath: dashboards.yaml
      volumes:
        - name: dashboards
          configMap:
            name: zhushou-grafana-dashboards
        - name: provisioning
          configMap:
            name: zhushou-grafana-provisioning
---
apiVersion: v1
kind: Service
metadata:
  name: zhushou-grafana
  labels:
    app: zhushou-grafana
spec:
  type: ClusterIP
  selector:
    app: zhushou-grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
