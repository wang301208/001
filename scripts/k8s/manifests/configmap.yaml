apiVersion: v1
kind: ConfigMap
metadata:
  name: zhushou-config
  labels:
    app: zhushou
data:
  zhushou.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.zhushou/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "助手 Assistant",
            "workspace": "~/.zhushou/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # 助手 Assistant

    You are a helpful AI assistant running in Kubernetes.
