apiVersion: v1
kind: Service
metadata:
  name: assistant
  labels:
    app: assistant
spec:
  type: ClusterIP
  selector:
    app: assistant
  ports:
    - name: gateway
      port: 3000
      targetPort: 3000
      protocol: TCP
