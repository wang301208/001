apiVersion: v1
kind: Service
metadata:
  name: zhushou
  labels:
    app: zhushou
spec:
  type: ClusterIP
  selector:
    app: zhushou
  ports:
    - name: gateway
      port: 3000
      targetPort: 3000
      protocol: TCP
